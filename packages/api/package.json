{
  "name": "api",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "test": "vitest",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings ./types/worker-configuration.d.ts",
    "db:migrate-local": "wrangler d1 migrations apply bb --local",
    "db:migrate": "wrangler d1 migrations apply bb --remote"
  },
  "dependencies": {
    "@hono/zod-validator": "^0.7.5",
    "bcryptjs": "^3.0.3",
    "hono": "^4.10.6",
    "totp-generator": "^2.0.0",
    "zod": "^4.1.13"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.10.10",
    "miniflare": "^4.20251118.1",
    "vitest": "^3.2.4",
    "wrangler": "^4.4.0"
  }
}